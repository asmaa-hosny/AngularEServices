using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

using EServicesWithAngular.DataStore;

using Microsoft.AspNetCore.Mvc;

namespace EServicesWithAngular.Controllers
{
    [Route("api/[controller]")]
    public class WorkflowController : BaseController
    {
        

        //[HttpGet]
        //[Route("GetDecisionList")]
        //public IActionResult GetDecisionList(string jobID, short NodeID, short EPC)
        //{
        //    var activity = KtaService.TakeActivityAsync(base.getUserSession(), jobID, NodeID, EPC);
        //    var decisions = RecordsKeepingManager.getDecisionList(activity.HelpText);
        //    return Ok(decisions);

        //}





    }
}